<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal">

    <EditText
        android:id="@+id/search_edit_text"
        android:layout_width="match_parent"
        android:layout_height="@dimen/size_search_panel"
        android:layout_marginStart="@dimen/size_twenty"
        android:layout_marginEnd="@dimen/size_twenty"
        android:lines="1"
        android:background="@drawable/rounded_edittext"
        android:fontFamily="@font/poppins_regular"
        android:hint="@string/search"
        android:textColorHint="@color/search_color"
        android:imeOptions="actionSearch"
        android:paddingStart="@dimen/size_twenty"
        android:paddingTop="@dimen/size_baby"
        android:paddingEnd="@dimen/size_twenty"
        android:textColor="@color/white"
        android:textSize="@dimen/text_size_medium"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/search_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/size_image_view_padding_end"
        android:src="@drawable/search_main"
        app:layout_constraintBottom_toBottomOf="@id/search_edit_text"
        app:layout_constraintEnd_toEndOf="@id/search_edit_text"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>
